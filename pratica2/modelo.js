// modelo.js

class Contato {
    constructor(nome, email, telefone) {
        this.nome = nome;
        this.email = email;
        this.telefone = telefone;
    }

    // Outros métodos da classe Contato podem ser adicionados aqui conforme necessário
}

module.exports = Contato; // Exporta a classe Contato para uso em outros arquivos
